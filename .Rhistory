setwd("E:/towork/study/r-studio/projects/time-series clustring")
data <- read.table(file.choose(), header=F, sep = "")
View(data)
View(data)
plot(data[,60], type = 'l')
j <- c(5,105, 205, 305,405, 505)
sample <- t(data[j,])
plot.ts(sample)
j <- c(5,105, 205, 305,405, 505)
sample <- t(data[j,])
plot.ts(sample,
main="Time -series Plot",
col='blue',
type= 'b')
n <- 10
s <- sample(1:100, n)
i <- c(s, 100+s, 200+s, 300+s, 400+s, 500+s)
d <- data[i, ]
str(d)
pattern <- c(rep('Normal',n),
rep('Cyclic',n),
rep('Increasing',n),
rep('Decreasing',n),
rep('Upward',n),
rep('downward',n))
rep(1:4, 2)
install.packages("dtw")
library(dtw)
distance <- dist(d, method = "DTW")
hc <- hclust(distance, method='average')
plot(hc,
lables=pattern,
cex=0.7,
hang=-1,
col='blue')
hc <- hclust(distance, method='average')
plot(hc,
lables= pattern,
cex=0.7,
hang=-1,
col='blue')
library(dtw)
distance <- dist(d, method = "DTW")
pattern <- c(rep('Normal',n),
rep('Cyclic',n),
rep('Increasing',n),
rep('Decreasing',n),
rep('Upward',n),
rep('downward',n))
hc <- hclust(distance, method='average')
plot(hc,
lables= pattern,
cex=0.7,
hang=-1,
col='blue')
pattern <- c(rep("Normal",n),
rep("Cyclic",n),
rep("Increasing",n),
rep("Decreasing",n),
rep("Upward",n),
rep("downward",n))
hc <- hclust(distance, method='average')
plot(hc,
lables= pattern,
cex=0.7,
hang=-1,
col='blue')
hc <- hclust(distance, method='average')
plot(hc,
lables= pattern,
cex=0.7,
hang=-1,
col='blue')
pattern <- c(rep('Normal',n),
rep('Cyclic',n),
rep('Increasing',n),
rep('Decreasing',n),
rep('Upward',n),
rep('downward',n))
library(dtw)
distance <- dist(d, method = "DTW")
hc <- hclust(distance, method='average')
plot(hc,
lables= pattern,
cex=0.7,
hang=-1,
col='blue')
hc <- hclust(distance, method='average')
plot(hc,
labels = pattern,
cex=0.7,
hang=-1,
col='blue')
rect.hclust(hc, k=4)
hc <- hclust(distance, method='average')
plot(hc,
labels = pattern,
cex=0.7,
hang=-1,
col='blue')
rect.hclust(hc, k=4)
rect.hclust(hc, k=4)
rect.hclust(hc, k=6)
rect.hclust(hc, k=2)
install.packages("hClust")
rect.hclust(hc, k=4)
hc <- hclust(distance, method='average')
plot(hc,
labels = pattern,
cex=0.7,
hang=-1,
col='blue')
rect.hclust(hc, k=4)
hc <- hclust(distance, method='average')
plot(hc,
labels = pattern,
cex=0.7,
hang=-1,
col='blue')
rect.hclust(hc, k=6)
hc <- hclust(distance, method='average')
plot(hc,
labels = pattern,
cex=0.7,
hang=-1,
col='blue')
rect.hclust(hc, k=4)
setwd("E:/towork/study/r-studio/projects/time-series clustring")
